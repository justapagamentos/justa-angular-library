import { MoneyMaskDirective } from './money-mask.directive';

describe('MoneyMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new MoneyMaskDirective();
    expect(directive).toBeTruthy();
  });
});
